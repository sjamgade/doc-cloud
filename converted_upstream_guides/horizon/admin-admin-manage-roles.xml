<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<section xmlns="http://docbook.org/ns/docbook" xmlns:xlink="http://www.w3.org/1999/xlink" xmlns:xi="http://www.w3.org/2001/XInclude" version="5.1">
  <title>Create and manage roles</title>
  <para>A role is a personality that a user assumes to perform a specific set
            of operations. A role includes a set of rights and privileges. A user
            assumes that role inherits those rights and privileges.</para>
  <note>
    <para>OpenStack Identity service defines a user’s role on a
                project, but it is completely up to the individual service
                to define what that role means. This is referred to as the
                service’s policy. To get details about what the privileges
                for each role are, refer to the <literal>policy.json</literal> file
                available for each service in the
                <literal>/etc/SERVICE/policy.json</literal> file. For example, the
                policy defined for OpenStack Identity service is defined
                in the <literal>/etc/keystone/policy.json</literal> file.</para>
  </note>
  <section>
    <title>Create a role</title>
    <procedure>
      <step>
        <para>Log in to the dashboard and select the <guilabel>admin</guilabel> project from the
                        drop-down list.</para>
      </step>
      <step>
        <para>On the <guilabel>Identity</guilabel> tab, click the <guilabel>Roles</guilabel> category.</para>
      </step>
      <step>
        <para>Click the <guilabel>Create Role</guilabel> button.</para>
        <para>In the <guilabel>Create Role</guilabel> window, enter a name for the role.</para>
      </step>
      <step>
        <para>Click the <guilabel>Create Role</guilabel> button to confirm your changes.</para>
      </step>
    </procedure>
  </section>
  <section>
    <title>Edit a role</title>
    <procedure>
      <step>
        <para>Log in to the dashboard and select the <guilabel>Identity</guilabel> project from the
                        drop-down list.</para>
      </step>
      <step>
        <para>On the <guilabel>Identity</guilabel> tab, click the <guilabel>Roles</guilabel> category.</para>
      </step>
      <step>
        <para>Click the <guilabel>Edit</guilabel> button.</para>
        <para>In the <guilabel>Update Role</guilabel> window, enter a new name for the role.</para>
      </step>
      <step>
        <para>Click the <guilabel>Update Role</guilabel> button to confirm your changes.</para>
      </step>
    </procedure>
    <note>
      <para>Using the dashboard, you can edit only the name assigned to
                    a role.</para>
    </note>
  </section>
  <section>
    <title>Delete a role</title>
    <procedure>
      <step>
        <para>Log in to the dashboard and select the <guilabel>Identity</guilabel> project from the
                        drop-down list.</para>
      </step>
      <step>
        <para>On the <guilabel>Identity</guilabel> tab, click the <guilabel>Roles</guilabel> category.</para>
      </step>
      <step>
        <para>Select the role you want to delete and click the <guilabel>Delete
                            Roles</guilabel> button.</para>
      </step>
      <step>
        <para>In the <guilabel>Confirm Delete Roles</guilabel> window, click <guilabel>Delete
                            Roles</guilabel> to confirm the deletion.</para>
        <para>You cannot undo this action.</para>
      </step>
    </procedure>
  </section>
</section>
